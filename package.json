{
  "name": "wrad",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test -w @wrad/core",
    "dev": "npm run dev -w @wrad/app",
    "build": "npm run build -w @wrad/app",
    "balance": "npm run balance -w @wrad/core",
    "snowball": "npm run snowball -w @wrad/core"
  }
}
