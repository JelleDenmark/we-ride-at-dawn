node_modules/
dist/
*.local
.env
.env.*
!.env.example
supabase/.temp/

# Working exploit PoCs against the live prod backend — never commit to the
# public repo, even after the underlying issue is fixed.
White_hat_Stealthrat.zip
